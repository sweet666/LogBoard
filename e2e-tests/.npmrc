# Always install optional (platform-native) packages.
# Without this, npm v7+ sometimes skips optional binaries (e.g.
# @rollup/rollup-darwin-arm64) when a package-lock.json was generated
# on a different OS — see https://github.com/npm/cli/issues/4828
optional=true
